# 2026-03-12 小红书做深执行说明

## 1. 手工验证清单

### 1) 收藏夹抓取成功路径

- 前置：Chrome 已启用 `--remote-debugging-port=9222`，并打开小红书收藏页
- 执行：`node scripts/extract_v4.js`
- 预期：
  - 终端显示 `Connected to Chrome`
  - `data/raw_notes.json` 更新
  - `output/` 下生成对应收藏内容

### 2) 单笔记保存成功路径

- 前置：Chrome 9222 已启用，存在可访问的笔记链接
- 执行：`node scripts/save_note.js <笔记链接>`
- 预期：
  - 生成 `.md` 文件
  - 输出 summary 中 `successCount` 递增

### 3) 评论抓取失败退化路径

- 前置：人为模拟评论接口失败或断网
- 执行：单笔记保存入口
- 预期：
  - 正文与图片仍然导出
  - `commentSummary` 退化为提示文本
  - report warnings 中包含评论相关 warning

### 4) OCR fallback 路径

- 前置：禁用 Vision OCR 或模拟 Vision OCR 失败
- 执行：单笔记保存入口，笔记含图片
- 预期：
  - OCR 走 Tesseract fallback
  - 图片 OCR 文本仍然出现

### 5) AI 失败退化路径

- 前置：AI key 缺失或手动关闭 AI
- 执行：单笔记保存入口
- 预期：
  - summary/tags 使用本地 fallback
  - report 中无崩溃

### 6) UI 入口成功路径

- 前置：启动 `启动小红书保存入口.bat`
- 执行：通过 UI 提交单笔记链接与收藏导出
- 预期：
  - UI 展示 `report` JSON
  - 任务状态切换正确

## 2. 自动化测试建议

完整回归：

```powershell
node --test scripts/ai/__tests__/*.test.js
```
