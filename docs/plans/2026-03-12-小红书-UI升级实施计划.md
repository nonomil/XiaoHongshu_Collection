# 小红书 UI 升级 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** UI 支持路径/命名/运行参数配置与结果展示增强，并将配置持久化到 `config/ui.json`，UI 运行时覆盖默认行为。

**Architecture:** 新增 UI 配置层 + 命名冲突解析器 + 运行时覆盖适配。UI 通过 `/api/ui-config` 读写配置，并在保存请求中传递覆盖参数，后端将覆盖参数注入保存流程。

**Tech Stack:** Node.js (http server), Vanilla JS UI, existing scripts (`save_note.js`, `extract_v4.js`, `ocr_and_write.js`).

---

### Task 1: 新增 UI 配置加载/保存模块

**Files:**
- Create: `scripts/lib/ui_config.js`
- Test: `scripts/ai/__tests__/ui_config.test.js`

**Step 1: Write the failing test**

```js
const { test } = require('node:test');
const assert = require('node:assert/strict');
const fs = require('fs');
const path = require('path');

const { loadUiConfig, mergeUiConfig, saveUiConfig } = require('../../lib/ui_config');

const tmpDir = path.join(__dirname, '..', '__tmp__', 'ui-config');
const cfgPath = path.join(tmpDir, 'ui.json');

function resetTmp() {
  fs.rmSync(tmpDir, { recursive: true, force: true });
  fs.mkdirSync(tmpDir, { recursive: true });
}

test('loadUiConfig returns defaults when missing', () => {
  resetTmp();
  const cfg = loadUiConfig({ configPath: cfgPath });
  assert.equal(cfg._missing, true);
  assert.ok(cfg.paths);
});

test('mergeUiConfig overlays user values', () => {
  const base = { paths: { saveLinksOutputRoot: 'A' }, runtime: { aiSummaryEnabled: true } };
  const override = { paths: { saveLinksOutputRoot: 'B' }, runtime: { aiSummaryEnabled: false } };
  const merged = mergeUiConfig(base, override);
  assert.equal(merged.paths.saveLinksOutputRoot, 'B');
  assert.equal(merged.runtime.aiSummaryEnabled, false);
});

test('saveUiConfig writes json file', () => {
  resetTmp();
  const payload = { paths: { saveLinksOutputRoot: 'X' } };
  saveUiConfig({ configPath: cfgPath, payload });
  const stored = JSON.parse(fs.readFileSync(cfgPath, 'utf-8'));
  assert.equal(stored.paths.saveLinksOutputRoot, 'X');
});
```

**Step 2: Run test to verify it fails**

Run: `node --test scripts/ai/__tests__/ui_config.test.js`
Expected: FAIL with module not found / functions missing.

**Step 3: Write minimal implementation**

```js
const fs = require('fs');
const path = require('path');

const DEFAULTS = {
  paths: {
    saveLinksOutputRoot: '',
    saveLinksImagesRoot: '',
    collectionOutputRoot: '',
    collectionRawPath: ''
  },
  naming: {
    conflictStrategy: 'content-aware',
    maxTitleLength: 80
  },
  runtime: {
    aiSummaryEnabled: true,
    visionOcrEnabled: true,
    ocrFallbackEnabled: true,
    openRouterTimeoutMs: 30000,
    visionOcrTimeoutMs: 60000,
    maxImagesPerNote: 12
  },
  ui: {
    showRawReport: true
  }
};

function mergeUiConfig(base, override) {
  const merged = JSON.parse(JSON.stringify(base || DEFAULTS));
  if (!override || typeof override !== 'object') return merged;
  for (const section of Object.keys(DEFAULTS)) {
    merged[section] = { ...merged[section], ...(override[section] || {}) };
  }
  return merged;
}

function loadUiConfig({ configPath }) {
  if (!configPath || !fs.existsSync(configPath)) {
    return { ...DEFAULTS, _missing: true };
  }
  try {
    const parsed = JSON.parse(fs.readFileSync(configPath, 'utf-8'));
    return mergeUiConfig(DEFAULTS, parsed);
  } catch (error) {
    return { ...DEFAULTS, _invalid: true, error };
  }
}

function saveUiConfig({ configPath, payload }) {
  const dir = path.dirname(configPath);
  fs.mkdirSync(dir, { recursive: true });
  fs.writeFileSync(configPath, JSON.stringify(payload, null, 2), 'utf-8');
}

module.exports = { DEFAULTS, loadUiConfig, mergeUiConfig, saveUiConfig };
```

**Step 4: Run test to verify it passes**

Run: `node --test scripts/ai/__tests__/ui_config.test.js`
Expected: PASS

**Step 5: Commit**

```bash
git add scripts/lib/ui_config.js scripts/ai/__tests__/ui_config.test.js
git commit -m "feat: add ui config loader"
```

---

### Task 2: 增加 Markdown 正文比对与冲突命名解析器

**Files:**
- Create: `scripts/lib/output_naming.js`
- Test: `scripts/ai/__tests__/output_naming.test.js`

**Step 1: Write the failing test**

```js
const { test } = require('node:test');
const assert = require('node:assert/strict');
const fs = require('fs');
const path = require('path');

const {
  stripFrontmatter,
  isSameBody,
  resolveMarkdownConflict
} = require('../../lib/output_naming');

const tmpDir = path.join(__dirname, '..', '__tmp__', 'output-naming');
const filePath = path.join(tmpDir, 'note.md');

function resetTmp() {
  fs.rmSync(tmpDir, { recursive: true, force: true });
  fs.mkdirSync(tmpDir, { recursive: true });
}

test('stripFrontmatter removes yaml header', () => {
  const text = '---\nkey: value\n---\n\nBody';
  assert.equal(stripFrontmatter(text), 'Body');
});

test('isSameBody compares only body content', () => {
  const a = '---\nkey: a\n---\n\nBody';
  const b = '---\nkey: b\n---\n\nBody';
  assert.equal(isSameBody(a, b), true);
});

test('resolveMarkdownConflict overwrites when body same', () => {
  resetTmp();
  fs.writeFileSync(filePath, '---\nkey: a\n---\n\nBody', 'utf-8');
  const resolved = resolveMarkdownConflict({
    filepath: filePath,
    nextMarkdown: '---\nkey: b\n---\n\nBody'
  });
  assert.equal(resolved, filePath);
});

test('resolveMarkdownConflict adds suffix when body differs', () => {
  resetTmp();
  fs.writeFileSync(filePath, '---\nkey: a\n---\n\nBody', 'utf-8');
  const resolved = resolveMarkdownConflict({
    filepath: filePath,
    nextMarkdown: '---\nkey: b\n---\n\nBody2'
  });
  assert.ok(resolved.endsWith('-1.md'));
});
```

**Step 2: Run test to verify it fails**

Run: `node --test scripts/ai/__tests__/output_naming.test.js`
Expected: FAIL

**Step 3: Write minimal implementation**

```js
const fs = require('fs');
const path = require('path');

function stripBom(text) {
  return String(text || '').replace(/^\uFEFF/, '');
}

function stripFrontmatter(text) {
  const value = stripBom(text).trimStart();
  if (!value.startsWith('---')) return stripBom(text);
  const endIndex = value.indexOf('\n---', 3);
  if (endIndex === -1) return stripBom(text);
  const rest = value.slice(endIndex + '\n---'.length);
  return rest.replace(/^\s+/, '');
}

function isSameBody(existing, nextMarkdown) {
  return stripFrontmatter(existing).trim() === stripFrontmatter(nextMarkdown).trim();
}

function resolveMarkdownConflict({ filepath, nextMarkdown }) {
  if (!fs.existsSync(filepath)) return filepath;
  const current = fs.readFileSync(filepath, 'utf-8');
  if (isSameBody(current, nextMarkdown)) return filepath;

  const ext = path.extname(filepath);
  const base = filepath.slice(0, -ext.length);
  let index = 1;
  while (true) {
    const candidate = `${base}-${index}${ext}`;
    if (!fs.existsSync(candidate)) return candidate;
    const existing = fs.readFileSync(candidate, 'utf-8');
    if (isSameBody(existing, nextMarkdown)) return candidate;
    index += 1;
  }
}

module.exports = { stripFrontmatter, isSameBody, resolveMarkdownConflict };
```

**Step 4: Run test to verify it passes**

Run: `node --test scripts/ai/__tests__/output_naming.test.js`
Expected: PASS

**Step 5: Commit**

```bash
git add scripts/lib/output_naming.js scripts/ai/__tests__/output_naming.test.js
git commit -m "feat: add markdown conflict resolver"
```

---

### Task 3: 链接保存流程支持 UI 路径/运行参数覆盖

**Files:**
- Modify: `scripts/lib/note_export.js`
- Modify: `scripts/save_note.js`
- Test: `scripts/ai/__tests__/note_export.test.js`

**Step 1: Write the failing test**

```js
// add a new test case to ensure writeSingleNoteMarkdown uses conflict resolver
// and that UI runtime overrides can disable ai summary
```

**Step 2: Run test to verify it fails**

Run: `node --test scripts/ai/__tests__/note_export.test.js`
Expected: FAIL

**Step 3: Write minimal implementation**

```js
// in note_export.js
const { resolveMarkdownConflict } = require('./output_naming');

function writeSingleNoteMarkdown({... , conflictStrategy }) {
  ...
  const markdown = generateMarkdown(...);
  const targetPath = conflictStrategy === 'content-aware'
    ? resolveMarkdownConflict({ filepath, nextMarkdown: markdown })
    : filepath;
  fs.writeFileSync(targetPath, `\uFEFF${markdown}`, 'utf-8');
  return targetPath;
}
```

```js
// in save_note.js
// accept options.uiRuntime and options.uiPaths to override outputRoot/imagesRoot
```

**Step 4: Run test to verify it passes**

Run: `node --test scripts/ai/__tests__/note_export.test.js`
Expected: PASS

**Step 5: Commit**

```bash
git add scripts/lib/note_export.js scripts/save_note.js scripts/ai/__tests__/note_export.test.js
git commit -m "feat: allow ui overrides for note export"
```

---

### Task 4: 收藏导出流程支持 UI 覆盖

**Files:**
- Modify: `scripts/extract_v4.js`
- Modify: `scripts/ocr_and_write.js`
- Test: `scripts/ai/__tests__/ui_server.test.js`

**Step 1: Write the failing test**

```js
// add test that ui_server passes custom raw path/output path to collection export
```

**Step 2: Run test to verify it fails**

Run: `node --test scripts/ai/__tests__/ui_server.test.js`
Expected: FAIL

**Step 3: Write minimal implementation**

```js
// extract_v4.js
const RAW_PATH = process.env.XHS_RAW_PATH || path.join(PROJECT_DIR, 'data', 'raw_notes.json');
```

```js
// ocr_and_write.js
const OUTPUT_ROOT = process.env.XHS_OUTPUT_ROOT || PATHS.outputDir;
const RAW_PATH = process.env.XHS_RAW_PATH || path.join(PATHS.dataDir, 'raw_notes.json');
```

**Step 4: Run test to verify it passes**

Run: `node --test scripts/ai/__tests__/ui_server.test.js`
Expected: PASS

**Step 5: Commit**

```bash
git add scripts/extract_v4.js scripts/ocr_and_write.js scripts/ai/__tests__/ui_server.test.js
git commit -m "feat: allow collection export path overrides"
```

---

### Task 5: UI Server 读写 UI 配置与透传覆盖参数

**Files:**
- Modify: `scripts/ui_server.js`
- Test: `scripts/ai/__tests__/ui_server.test.js`

**Step 1: Write the failing test**

```js
// add tests for GET/POST /api/ui-config and save-links/collection receiving overrides
```

**Step 2: Run test to verify it fails**

Run: `node --test scripts/ai/__tests__/ui_server.test.js`
Expected: FAIL

**Step 3: Write minimal implementation**

```js
// add endpoints:
// GET /api/ui-config -> return merged config
// POST /api/ui-config -> save config
// In save-links and save-collection: accept payload.uiConfig, merge with defaults
// and pass overrides to saveLinksText / runCollectionExport
```

**Step 4: Run test to verify it passes**

Run: `node --test scripts/ai/__tests__/ui_server.test.js`
Expected: PASS

**Step 5: Commit**

```bash
git add scripts/ui_server.js scripts/ai/__tests__/ui_server.test.js
git commit -m "feat: add ui config endpoints"
```

---

### Task 6: UI 前端升级与编码修复

**Files:**
- Modify: `ui/index.html`
- Modify: `ui/app.js`
- Modify: `ui/styles.css`

**Step 1: Write the failing test**

```js
// (UI is static; rely on manual verification checklist instead)
```

**Step 2: Implement minimal UI changes**

- 新增配置面板与保存按钮
- 调用 `/api/ui-config` 加载/保存
- 提交时携带 `uiConfig` 覆盖
- 结果区展示：摘要 + 文件清单 + 原始 JSON 折叠
- 全面修复中文乱码（使用 UTF-8 文案）

**Step 3: Manual verification**

- 能加载配置并回填表单
- 保存配置后刷新仍保留
- 运行保存时路径和参数按 UI 生效

**Step 4: Commit**

```bash
git add ui/index.html ui/app.js ui/styles.css
git commit -m "feat: upgrade ui with config panels"
```

---

### Task 7: 全量回归

**Step 1: Run tests**

Run: `node --test scripts/ai/__tests__/*.test.js`
Expected: PASS

**Step 2: Commit (if needed)**

```bash
git status -sb
```

---

Plan complete and saved to `docs/plans/2026-03-12-小红书-UI升级实施计划.md`. Two execution options:

1. Subagent-Driven (this session) - I dispatch fresh subagent per task, review between tasks, fast iteration
2. Parallel Session (separate) - Open new session with executing-plans, batch execution with checkpoints

Which approach?
